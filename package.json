{
  "name": "bank-account",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "1.0.7"
  },
  "dependencies": {
    "fbemitter": "^2.1.1",
    "flux": "^3.1.2",
    "react": "^15.6.0",
    "react-dom": "^15.6.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "yarn run build && surge -p build -d bank-account.surge.sh"
  }
}
